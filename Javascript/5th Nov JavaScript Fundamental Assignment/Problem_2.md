##### The comma operator in javascript allows you to evaluate multiple expressions. It is used to separate the multiple expressions within a larger expression or statement.

```
Example
let x=1, y=2, z=3
```